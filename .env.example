# Choose ONE run mode by copying the correct env file below.

# Common
RABBITMQ_PASSWORD=brocklee-dev-password

# ── Local mode (SQL Server container) ───────────────────────────────────────
# Used when you run: docker compose --profile local up --build
MSSQL_SA_PASSWORD=Str0ng!Passw0rd
ConnectionStrings__DefaultConnection=Server=mssql,1433;Database=BrockLee;User Id=sa;Password=Str0ng!Passw0rd;Encrypt=True;TrustServerCertificate=True;

# ── Azure mode (Azure SQL) ──────────────────────────────────────────────────
# Used when you run: docker compose up --build
# Uncomment and set your actual Azure SQL connection string:
# ConnectionStrings__DefaultConnection=Server=<server>.database.windows.net;Database=<db>;User Id=<user>;Password=<password>;Encrypt=True;TrustServerCertificate=False;